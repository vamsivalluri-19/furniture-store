//KNAPSACK PROBLEM DAA
#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;

int knapSack(int W, const vector<int>& weights, const vector<int>& values, int n) {
    vector<vector<int>> dp(n + 1, vector<int>(W + 1, 0));

    for (int i = 1; i <= n; ++i) {
        for (int w = 1; w <= W; ++w) {
            if (weights[i - 1] > w) {
                dp[i][w] = dp[i - 1][w];
            } else {
                dp[i][w] = max(dp[i - 1][w], values[i - 1] + dp[i - 1][w - weights[i - 1]]);
            }
        }
    }

    return dp[n][W];
}

int main() {
    vector<int> values = {3, 4, 5};     // Item values
    vector<int> weights = {2, 3, 4};    // Item weights
    int W = 5;                          // Knapsack capacity
    int n = values.size();              // Number of items

    int maxValue = knapSack(W, weights, values, n);
    cout << "Maximum value in knapsack of capacity " << W << " is: " << maxValue << endl;

    return 0;
}
